# Feedbax portal

1. Copy `.env.example` to `.env` and configure the server-only values.
2. Run `npx feedbax doctor`.
3. Run `pnpm dev` (or `npm run dev`).

Email identity is capture-only and does not verify ownership. Notion-backed interaction uniqueness is best effort.
