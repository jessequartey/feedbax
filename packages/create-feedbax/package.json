{
  "name": "create-feedbax",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "create-feedbax": "dist/cli.js"
  },
  "files": [
    "dist",
    "template",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "node scripts/prepare-template.mjs && pnpm build",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test",
    "test": "vitest run"
  },
  "dependencies": {
    "@feedbax/identity": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.10.13"
  }
}
