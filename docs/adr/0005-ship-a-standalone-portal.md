---
status: accepted
---

# Ship a standalone portal

Version 0.2.0 is a finished standalone application deployed to its own Cloudflare Worker and custom domain, with an HTTP submission endpoint for a Product Team's SaaS. Installing Feedbax inside an existing application and an embeddable package are deferred; the framework implementation is decided separately.
